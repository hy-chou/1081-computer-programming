#include <cmath>
#include <curses.h>

/*int	main(void)
{
	initscr();
	mvprintw(10, 10, "Hello world!");
	refresh();
	for (int i = 0; i < 50; i++)
	{
		mvaddch(10 - 8 * sin(i / 3.0), 25 + i, 'A' + i);
		refresh();
		napms(100);
	}
	getch();
	endwin();
	return (0);
}
*/